// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"go-gin/database/ent/category_questions"
	"go-gin/database/ent/predicate"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// CategoryQuestionsQuery is the builder for querying Category_Questions entities.
type CategoryQuestionsQuery struct {
	config
	ctx        *QueryContext
	order      []category_questions.OrderOption
	inters     []Interceptor
	predicates []predicate.Category_Questions
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the CategoryQuestionsQuery builder.
func (cqq *CategoryQuestionsQuery) Where(ps ...predicate.Category_Questions) *CategoryQuestionsQuery {
	cqq.predicates = append(cqq.predicates, ps...)
	return cqq
}

// Limit the number of records to be returned by this query.
func (cqq *CategoryQuestionsQuery) Limit(limit int) *CategoryQuestionsQuery {
	cqq.ctx.Limit = &limit
	return cqq
}

// Offset to start from.
func (cqq *CategoryQuestionsQuery) Offset(offset int) *CategoryQuestionsQuery {
	cqq.ctx.Offset = &offset
	return cqq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (cqq *CategoryQuestionsQuery) Unique(unique bool) *CategoryQuestionsQuery {
	cqq.ctx.Unique = &unique
	return cqq
}

// Order specifies how the records should be ordered.
func (cqq *CategoryQuestionsQuery) Order(o ...category_questions.OrderOption) *CategoryQuestionsQuery {
	cqq.order = append(cqq.order, o...)
	return cqq
}

// First returns the first Category_Questions entity from the query.
// Returns a *NotFoundError when no Category_Questions was found.
func (cqq *CategoryQuestionsQuery) First(ctx context.Context) (*Category_Questions, error) {
	nodes, err := cqq.Limit(1).All(setContextOp(ctx, cqq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{category_questions.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (cqq *CategoryQuestionsQuery) FirstX(ctx context.Context) *Category_Questions {
	node, err := cqq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Category_Questions ID from the query.
// Returns a *NotFoundError when no Category_Questions ID was found.
func (cqq *CategoryQuestionsQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = cqq.Limit(1).IDs(setContextOp(ctx, cqq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{category_questions.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (cqq *CategoryQuestionsQuery) FirstIDX(ctx context.Context) int {
	id, err := cqq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Category_Questions entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Category_Questions entity is found.
// Returns a *NotFoundError when no Category_Questions entities are found.
func (cqq *CategoryQuestionsQuery) Only(ctx context.Context) (*Category_Questions, error) {
	nodes, err := cqq.Limit(2).All(setContextOp(ctx, cqq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{category_questions.Label}
	default:
		return nil, &NotSingularError{category_questions.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (cqq *CategoryQuestionsQuery) OnlyX(ctx context.Context) *Category_Questions {
	node, err := cqq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Category_Questions ID in the query.
// Returns a *NotSingularError when more than one Category_Questions ID is found.
// Returns a *NotFoundError when no entities are found.
func (cqq *CategoryQuestionsQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = cqq.Limit(2).IDs(setContextOp(ctx, cqq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{category_questions.Label}
	default:
		err = &NotSingularError{category_questions.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (cqq *CategoryQuestionsQuery) OnlyIDX(ctx context.Context) int {
	id, err := cqq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of Category_QuestionsSlice.
func (cqq *CategoryQuestionsQuery) All(ctx context.Context) ([]*Category_Questions, error) {
	ctx = setContextOp(ctx, cqq.ctx, "All")
	if err := cqq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*Category_Questions, *CategoryQuestionsQuery]()
	return withInterceptors[[]*Category_Questions](ctx, cqq, qr, cqq.inters)
}

// AllX is like All, but panics if an error occurs.
func (cqq *CategoryQuestionsQuery) AllX(ctx context.Context) []*Category_Questions {
	nodes, err := cqq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Category_Questions IDs.
func (cqq *CategoryQuestionsQuery) IDs(ctx context.Context) (ids []int, err error) {
	if cqq.ctx.Unique == nil && cqq.path != nil {
		cqq.Unique(true)
	}
	ctx = setContextOp(ctx, cqq.ctx, "IDs")
	if err = cqq.Select(category_questions.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (cqq *CategoryQuestionsQuery) IDsX(ctx context.Context) []int {
	ids, err := cqq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (cqq *CategoryQuestionsQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, cqq.ctx, "Count")
	if err := cqq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, cqq, querierCount[*CategoryQuestionsQuery](), cqq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (cqq *CategoryQuestionsQuery) CountX(ctx context.Context) int {
	count, err := cqq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (cqq *CategoryQuestionsQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, cqq.ctx, "Exist")
	switch _, err := cqq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (cqq *CategoryQuestionsQuery) ExistX(ctx context.Context) bool {
	exist, err := cqq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the CategoryQuestionsQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (cqq *CategoryQuestionsQuery) Clone() *CategoryQuestionsQuery {
	if cqq == nil {
		return nil
	}
	return &CategoryQuestionsQuery{
		config:     cqq.config,
		ctx:        cqq.ctx.Clone(),
		order:      append([]category_questions.OrderOption{}, cqq.order...),
		inters:     append([]Interceptor{}, cqq.inters...),
		predicates: append([]predicate.Category_Questions{}, cqq.predicates...),
		// clone intermediate query.
		sql:  cqq.sql.Clone(),
		path: cqq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
func (cqq *CategoryQuestionsQuery) GroupBy(field string, fields ...string) *CategoryQuestionsGroupBy {
	cqq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &CategoryQuestionsGroupBy{build: cqq}
	grbuild.flds = &cqq.ctx.Fields
	grbuild.label = category_questions.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
func (cqq *CategoryQuestionsQuery) Select(fields ...string) *CategoryQuestionsSelect {
	cqq.ctx.Fields = append(cqq.ctx.Fields, fields...)
	sbuild := &CategoryQuestionsSelect{CategoryQuestionsQuery: cqq}
	sbuild.label = category_questions.Label
	sbuild.flds, sbuild.scan = &cqq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a CategoryQuestionsSelect configured with the given aggregations.
func (cqq *CategoryQuestionsQuery) Aggregate(fns ...AggregateFunc) *CategoryQuestionsSelect {
	return cqq.Select().Aggregate(fns...)
}

func (cqq *CategoryQuestionsQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range cqq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, cqq); err != nil {
				return err
			}
		}
	}
	for _, f := range cqq.ctx.Fields {
		if !category_questions.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if cqq.path != nil {
		prev, err := cqq.path(ctx)
		if err != nil {
			return err
		}
		cqq.sql = prev
	}
	return nil
}

func (cqq *CategoryQuestionsQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Category_Questions, error) {
	var (
		nodes = []*Category_Questions{}
		_spec = cqq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Category_Questions).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Category_Questions{config: cqq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, cqq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (cqq *CategoryQuestionsQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := cqq.querySpec()
	_spec.Node.Columns = cqq.ctx.Fields
	if len(cqq.ctx.Fields) > 0 {
		_spec.Unique = cqq.ctx.Unique != nil && *cqq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, cqq.driver, _spec)
}

func (cqq *CategoryQuestionsQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(category_questions.Table, category_questions.Columns, sqlgraph.NewFieldSpec(category_questions.FieldID, field.TypeInt))
	_spec.From = cqq.sql
	if unique := cqq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if cqq.path != nil {
		_spec.Unique = true
	}
	if fields := cqq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, category_questions.FieldID)
		for i := range fields {
			if fields[i] != category_questions.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := cqq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := cqq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := cqq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := cqq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (cqq *CategoryQuestionsQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(cqq.driver.Dialect())
	t1 := builder.Table(category_questions.Table)
	columns := cqq.ctx.Fields
	if len(columns) == 0 {
		columns = category_questions.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if cqq.sql != nil {
		selector = cqq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if cqq.ctx.Unique != nil && *cqq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range cqq.predicates {
		p(selector)
	}
	for _, p := range cqq.order {
		p(selector)
	}
	if offset := cqq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := cqq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// CategoryQuestionsGroupBy is the group-by builder for Category_Questions entities.
type CategoryQuestionsGroupBy struct {
	selector
	build *CategoryQuestionsQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (cqgb *CategoryQuestionsGroupBy) Aggregate(fns ...AggregateFunc) *CategoryQuestionsGroupBy {
	cqgb.fns = append(cqgb.fns, fns...)
	return cqgb
}

// Scan applies the selector query and scans the result into the given value.
func (cqgb *CategoryQuestionsGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, cqgb.build.ctx, "GroupBy")
	if err := cqgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*CategoryQuestionsQuery, *CategoryQuestionsGroupBy](ctx, cqgb.build, cqgb, cqgb.build.inters, v)
}

func (cqgb *CategoryQuestionsGroupBy) sqlScan(ctx context.Context, root *CategoryQuestionsQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(cqgb.fns))
	for _, fn := range cqgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*cqgb.flds)+len(cqgb.fns))
		for _, f := range *cqgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*cqgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := cqgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// CategoryQuestionsSelect is the builder for selecting fields of CategoryQuestions entities.
type CategoryQuestionsSelect struct {
	*CategoryQuestionsQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (cqs *CategoryQuestionsSelect) Aggregate(fns ...AggregateFunc) *CategoryQuestionsSelect {
	cqs.fns = append(cqs.fns, fns...)
	return cqs
}

// Scan applies the selector query and scans the result into the given value.
func (cqs *CategoryQuestionsSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, cqs.ctx, "Select")
	if err := cqs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*CategoryQuestionsQuery, *CategoryQuestionsSelect](ctx, cqs.CategoryQuestionsQuery, cqs, cqs.inters, v)
}

func (cqs *CategoryQuestionsSelect) sqlScan(ctx context.Context, root *CategoryQuestionsQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(cqs.fns))
	for _, fn := range cqs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*cqs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := cqs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
